kind = "web"
previewPath = "/"
title = "BrandOps"
version = "1.0.0"
id = "artifacts/brandops"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20412

[services.development]
run = "pnpm --filter @workspace/brandops run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/brandops", "run", "build" ]
publicDir = "artifacts/brandops/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20412"
BASE_PATH = "/"
